import Vue from 'vue';
import Router from 'vue-router';

Vue.use(Router);

const routes = [
  // Define your routes here
];

const router = new Router({
  mode: 'history',
  routes,
});

export default router;